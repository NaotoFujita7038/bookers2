<div class="container">
  <div class="row">
  <div class="col-md-3">
      
      <%= render 'users/user', book: @book,user: @user %>
  </div>
<div class="col-md-8 offset-md-1"> 
<h2>Book detail</h2>
<table class=table "table">
<thead>
<tr>

</thead>
<tbody> 
<td> 
 <%= image_tag @user.get_profile_image(100,100) %>
  
 
   <%= link_to user_path(@user.id) do %>
     <br><%= @user.name  %></td>
      
    <% end %>
<td><%=link_to @book.title,book_path(@book.id) %></td>
<td><%= @book.body %></td>
<% if @user.id == current_user.id %>
<td><%= link_to "Edit", edit_book_path(@book.id), class: "btn btn-success btn-sm btn-block mb-3"%></td>
<td><%= link_to "Destroy", book_path(@book.id),class:"btn btn-danger btn-sm btn-block mb-3", method: :delete, "data-confirm" => "本当に削除しますか？" %>
</td>
<% end %>
</tr>
</tbody>
</table>
</div>
</div>
</div>
         
         
     